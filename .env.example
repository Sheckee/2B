# Railway injects PORT automatically. Use this for local testing only.
PORT=3000

# The Obsidian Mobile plugin must default to offline-only behavior.
OFFLINE_ONLY=true

# Optional future companion service settings. Do not store API keys here.
MODEL_DIR=/data/models
INDEX_DIR=/data/index
LOG_LEVEL=info
